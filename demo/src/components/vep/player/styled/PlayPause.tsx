import { styled } from "styled-components";

export const PlayPause = styled.div`
  margin-left: auto;
  margin-right: auto;
  svg {
    &:hover {
      cursor: pointer;
      transform: scale(1.1);
    }
  }
`;
